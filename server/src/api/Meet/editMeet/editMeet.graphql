type Mutation {
    editMeet(
        id: String!
        intro: String
        date: String
        isPublic: Boolean
        creator: String
        location: String
        maxparticipants: Int
    ): Meet!
}