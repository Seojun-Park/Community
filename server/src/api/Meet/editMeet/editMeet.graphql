type Mutation {
    editMeet(
        id: String!
        intro: String
        date: String
        time: String
        isPublic: Boolean
        location: String
        maxparticipants: Int
    ): Meet!
}