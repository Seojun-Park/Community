import React, { useState } from "react";
// import dublinBusClient from 'dublinbus-client'

export default data => {
    console.log(data);

    // dublinBusClient.realtimeBusInformation.getInformationByStopId(515).then(console.log)
};
